export default defineAppConfig({
  ui: {
    header: {
      slots: {
        title:
          "flex-1 focus-visible:outline-primary font-bold text-xl text-highlighted items-end gap-1.5",
      },
    },
  },
});
